{
  "name": "serverless-mailer",
  "version": "1.0.0",
  "description": "A Serverless service using AWS Lambda for submitting contact forms on static sites. It sends AWS Simple Email Service emails to verified addresses. Written in Python.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "serverless-python-requirements": "^4.2.5"
  }
}
